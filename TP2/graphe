library(ggplot2)

perf <- read.table("perf_inser_crois.dat", header = TRUE)

ggplot(perf,aes(x=taille, y=temps)) + geom_point() + geom_smooth() + ggtitle("Temps tri insertion sur tableau croissant")

ggsave("graphe_inser_croissant_temps.png")

