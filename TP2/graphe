library(ggplot2)

perf <- read.table("perf_inser_crois.dat", header = TRUE)

ggplot(perf,aes(x=taille, y=temps)) + geom_point() + geom_smooth() + ggtitle("Temps tri croissant sur tableau aléatoire")

ggsave("graphe_inser_croissant_+tests.png")

