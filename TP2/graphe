library(ggplot2)

perf <- read.table("perf_radix_alea.dat", header = TRUE)

ggplot(perf,aes(x=taille, y=mem)) + geom_point() + geom_smooth() + ggtitle("Consommation mémoire tri radix sur tableau aléatoire")

ggsave("graphe_radix_aleatoire_mem.png")

